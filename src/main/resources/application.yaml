spring:
  application:
    name: heating-service
    version: @project.version@
  profiles:
    active: home
  webflux:
    base-path: "/home"

server:
  port: 6200

logbook:
  format:
    style: http

logging:
  level:
    org.zalando.logbook: trace

jobs:
  hot-water:
    poolingInterval: PT30s

shelly:
  sensor:
    uni-hot-water:
      scheme: http
      host: 10.78.30.47

---

spring:
  profiles:
    active: local

server:
  port: 6002
management:
  server:
    port: 8002